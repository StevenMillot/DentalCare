# Dossier de secrets locaux (non versionné)

Ce dossier est destiné à contenir la clé de compte de service Google utilisée par le
serveur MCP `ga4-search-console` (voir `.cursor/mcp.json` et `GUIDE-MCP-SEARCH-CONSOLE-GA4.md`).

Placez ici le fichier téléchargé depuis Google Cloud Console :

```
.cursor/secrets/ga4-gsc-service-account.json
```

⚠️ **Ce fichier ne doit jamais être commit.** Il est exclu par `.gitignore`.
